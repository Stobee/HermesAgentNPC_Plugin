# Claude Code 작업 프롬프트 — Hermes 에이전트 서버 (Windows)

이 프롬프트는 **Windows의 Hermes 서버 프로젝트**에서 Claude Code로 작업할 때 사용하세요.
UE5 플러그인 쪽 작업은 이 저장소에서 별도로 진행되므로, 서버 세션은 클라이언트 코드를
볼 필요도, 건드릴 필요도 없습니다.

> **서버 OS는 프로토콜과 무관합니다.** 계약은 TCP 위의 길이 프리픽스 프레이밍뿐이라 어느
> 플랫폼에서 구현해도 동일하게 동작합니다. 이 프롬프트는 Windows 기준으로 쓰여 있고,
> 플랫폼에 따라 달라지는 부분(경로, 셸, 인증서 도구, 서비스 등록)만 Windows 방식으로 안내합니다.

**시작 전 필수**: **`ue5-socket-protocol.md`**(와이어 계약 전체)를 이 프롬프트와 함께 첨부할 것.
**이 프롬프트와 그 문서 두 개면 작업을 시작할 수 있습니다** — UE5 클라이언트 코드를 볼 필요 없습니다.
프롬프트와 스펙이 어긋나 보이면 **스펙이 이깁니다.**

> **더 읽으면 좋은 것 (선택)**: `docs/superpowers/specs/2026-07-28-hermes-settings-globalization-design.md`의
> §10.5~§10.6. 이 프롬프트에 옮겨 담은 결정들의 **근거와 배경**이 들어 있습니다. 결정 자체는
> 아래에 다 있으므로 없어도 진행할 수 있지만, "왜 이렇게 정했나"가 궁금해지면 그쪽을 보세요.

서버 프로젝트 경로: `[프로젝트 경로 입력]` (예: `C:\Work\HermesServer`)

**구현 언어·런타임은 자유롭게 고르세요.** 프로토콜 계층은 TCP 소켓과 JSON만 요구하고,
llama-server와는 HTTP로 붙으므로 Windows에서 돌아가는 것이면 무엇이든 됩니다.
다만 고를 때 다음 세 가지를 만족하는지 확인하세요:
**(1)** 바이트 단위 소켓 I/O(길이 프리픽스 프레이밍을 직접 다뤄야 함),
**(2)** TLS 서버 소켓 또는 리버스 프록시 조합,
**(3)** 오래 열려 있는 소켓과 llama-server로의 HTTP/SSE를 동시에 다룰 수 있는 동시성 모델.

---

## 프로젝트 개요

언리얼 엔진 5로 만든 비서형 NPC 한 개체와 TCP 소켓으로 통신하는 **Hermes 에이전트 서버**를 구현합니다.
플레이어의 발화를 받아 LLM으로 응답을 만들고, 필요하면 게임 내 행동(이동·인벤토리 등)을 NPC에게 지시합니다.
**대화 기억은 서버가 담당합니다** — 클라이언트는 자격 증명만 보관합니다.

```
UE5 플러그인  ──소켓 프로토콜──▶  Hermes 에이전트  ──HTTP/SSE──▶  llama-server
   (범용)          (공개 계약)         (이 작업)                    (LLM 백엔드)
```

**소켓 프로토콜 계층을 재구축하는 작업입니다.** 에이전트는 Hermes로, LLM 백엔드는
`llama-server`로 **고정**입니다. 갈아끼울 수 있는 것은 모델 파일과 그 파라미터이지
아키텍처가 아닙니다.

**기존 서버 코드가 있더라도 프로토콜 계층은 개조하지 말고 첨부한 스펙만 보고 새로 쓰세요.**
스펙에 없는 동작을 기존 구현에서 물려받으면 클라이언트가 조용히 오작동합니다.

**호환 모드나 버전 협상은 만들지 마세요.** `identify`는 `protocol_version: 2`를 싣고, 그 외 값은
거부합니다(§5). 인증 없이 동작하는 경로는 자격 증명을 두는 의미를 없앱니다.

---

## 절대 규칙 — 프로토콜 문서가 유일한 계약

이 프롬프트는 작업 지시이고, **정확한 와이어 계약은 항상 `ue5-socket-protocol.md`가 이깁니다.**
프롬프트와 문서가 어긋나 보이면 문서를 따르고 그 사실을 알려주세요.

특히 다음은 **임의로 바꾸지 마세요.** 클라이언트가 이미 그렇게 구현되어 있어 조용히 깨집니다:

1. **프레이밍** — 4바이트 big-endian 길이 프리픽스 + UTF-8 JSON, 바디 최대 1 MiB (§1).
   길이 기준으로 파싱하고, 패킷 경계를 신뢰하지 마세요.
2. **프레임에 필드를 임의로 추가하지 마세요.** 클라이언트는 미지 필드를 **무시**합니다(§2).
   에러가 나는 게 아니라 조용히 무시되므로, 새 필드로 동작을 바꾸려는 설계는 전부 실패합니다.
3. **`npc_id` 같은 대상 지정 필드를 만들지 마세요.** 이 프로토콜은 **NPC 한 개체**만 다룹니다
   (스펙 "Scope" 절). 하나의 연결이 곧 하나의 NPC입니다.
4. **`id`는 받은 그대로 에코**됩니다. 클라이언트가 이 `id`로 타이머와 중복 가드를 겁니다.
5. **`text`는 평문 UTF-8**입니다. 클라이언트는 받은 문자열을 그대로 대사창에 표시합니다.
   **마크다운·툴 태그·사고 과정 제거는 서버 책임**입니다.
6. **액션 카탈로그 4종은 고정**입니다(§6). 서버가 새 `command`를 만들어 보내도 클라이언트는
   `unsupported command`로 거부합니다. 카탈로그 확장은 플러그인 버전 업 사안입니다.
7. **연결은 클라이언트만 엽니다.** 서버가 UE5로 접속할 방법은 없습니다. NPC에게 먼저 말을 걸려면
   그 클라이언트가 접속해 있어야 합니다.

---

## 목표

### 1. 전송 계층

- TCP `:8770` 리슨. **TLS 1.2+ 종단** — 직접 종단하거나 리버스 프록시 뒤에 두어도 됩니다(§0).
  Windows라면 Caddy나 nginx 모두 그대로 동작하고, IIS의 ARR(Application Request Routing)로
  종단해도 됩니다. 프록시를 쓰면 인증서와 핀은 **프록시 쪽에 속합니다**
- 자체 서명 인증서를 쓴다면 **SPKI SHA-256 핀을 공개**할 것.
  인증서가 아니라 **공개키**에 핀을 겁니다 — 같은 키로 갱신하면 기존 핀이 유지됩니다.
  스펙 §0의 산출 명령은 POSIX 파이프라인이므로, Windows에서는 §0에 함께 실린
  PowerShell 변형이나 Git Bash를 쓰세요
- 개발용 인증서는 `New-SelfSignedCertificate`로 만들 수 있습니다. 다만 **`bUseTLS`가 켜진
  클라이언트는 평문으로 폴백하지 않으므로**, TLS를 켜는 순간부터 인증서가 반드시 유효해야 합니다
- 방화벽에 인바운드 규칙이 필요합니다: `New-NetFirewallRule -DisplayName "Hermes 8770"
  -Direction Inbound -LocalPort 8770 -Protocol TCP -Action Allow`.
  **접속이 안 될 때 가장 흔한 원인이 이것입니다** — 프로토콜을 의심하기 전에 먼저 확인하세요
- 길이 프리픽스 프레이밍 구현. 1 MiB 초과 또는 JSON 파싱 실패는 `bad_frame`으로 **연결 종료**

### 2. 신원과 세션 (§3)

- `identify`에 자격 증명이 **없으면** `player_id`와 `session_token`을 발급하고 매핑을 **영구 저장**.
  재시작 후에도 유지되어야 합니다 — 잃으면 모든 플레이어가 잠깁니다
- `session_token`은 고엔트로피이며 **`player_id`에서 유도 불가능**해야 합니다
- 자격 증명이 **있으면** 쌍으로 검증. 불일치거나 한쪽만 왔으면 `not_authorized` 후 종료.
  **`player_id`만 알아도 세션에 접근되면 안 됩니다** — 자격 증명을 쌍으로 두는 이유입니다
- `identified`는 **재접속 시에도 두 값을 모두 실어** 보냅니다(§4.2).
  이 값이 빠지면 클라이언트는 "인증되지 않은 세션"으로 판단하고 연결을 끊습니다
- **세션 탈취**(§3.4): 같은 신원으로 새 연결이 오면 **새 연결이 이깁니다**.
  기존 연결에 `error{code:"session_taken_over"}`를 **보내고 나서** 닫으세요.
  조용히 끊으면 쫓겨난 클라이언트가 재접속해 세션을 되뺏고, 두 인스턴스가 서로를 영원히 걷어찹니다

### 3. 대화 계층 (§3.6, §4.3~4.4, §4.9)

- **턴 직렬화**: 세션당 한 번에 한 턴, 도착 순서대로. **두 턴의 프레임을 교차 전송하지 마세요.**
  클라이언트는 대화창 하나에 대기 상태 하나만 표현하므로 교차 스트리밍은 렌더링할 방법이 없습니다
- 플레이어는 응답을 기다리지 않고 연속 발화할 수 있습니다. **정상 트래픽이지 어뷰징이 아닙니다.**
  대기 큐에 상한을 두고 초과분은 `rate_limited`를 **해당 `chat.id`와 함께** 회신하세요
- **접수한 `chat`은 반드시 종료 프레임 하나로 끝냅니다** — `chat_response`, 또는 그 `id`를 단 `error`.
  조용히 버리면 클라이언트가 60초를 기다립니다
- `chat_delta`로 스트리밍하되 **~50ms 또는 몇 토큰 단위로 배칭**하세요. 토큰당 한 프레임은
  클라이언트의 틱 예산(64프레임/틱)과 인바운드 상한(1024)을 정상 동작 중에 압박합니다.
  상한에 닿으면 클라이언트가 **연결을 끊습니다**
- `chat_response.text`는 **잔여분이 아니라 전체 답변**입니다. 델타로 다 보냈어도 전문을 보내세요.
  클라이언트가 이 값으로 화면을 교체하므로 델타 유실이 자가 치유됩니다

### 4. 액션 (§4.5, §4.6, §4.10)

- `action_request`는 **언제든 푸시 가능**합니다 — 턴 중간에도, 대화와 무관하게도.
  클라이언트는 무조건 처리하고 같은 `id`로 회신합니다
- **`action_result`는 접수 확인이지 완료가 아닙니다.** 15초 안에 반드시 옵니다
- 장기 실행 액션(`move_to`)은 나중에 `action_event`로 완료/실패가 옵니다.
  **이것을 tool observation으로 에이전트에 되먹이세요** — 사용자 턴이 아니라 게임이 알려주는 관측입니다
- `action_event` 없이 끝난 액션의 결과는 **"알 수 없음"**입니다. 성공으로 간주하지 마세요
- 모르는 `id`의 `action_event`는 **에러로 답하지 말고 버리세요**. 서버 재시작 후 정상적으로 발생합니다.
  귀속할 수 없는 액션에 대해 관측을 지어내지 마세요

### 5. 모듈 경계

다음 다섯 축을 분리하세요. 이후 교체·확장이 이 경계를 따라 일어납니다.

| 축 | 책임 |
|---|---|
| **전송/세션** | TLS 종단, 소켓 수락, 프레이밍, 자격 증명 검증, `player_id` ↔ 연결 바인딩, 재접속 시 세션 복원 |
| **신원 저장소** | `player_id` ↔ `session_token` 매핑. **재시작 후에도 유지**되어야 플레이어가 잠기지 않음 |
| **대화 저장소** | `chat_id`별 히스토리. 분리하면 서버 재시작에도 맥락이 유지됨 |
| **LLM 백엔드** | llama-server 호출을 **한 모듈 뒤에** 둘 것. 추상화 계층을 미리 설계하라는 뜻이 아니라, llama 관련 호출이 코드베이스 전역에 퍼지지 않게만 하라는 뜻 |
| **액션 카탈로그** | 화이트리스트 명령 정의와 LLM 툴 스키마를 **한 곳에서 선언**. 스펙 §6·서버 툴 정의·UE 핸들러 세 곳을 손으로 맞추다 어긋나는 것이 실제로 있었던 사고다 |

### 6. llama-server 연동

아래는 프로토콜 계약과 직접 맞물리는 것들입니다.

- **툴 출력을 디코딩 단계에서 강제하세요** (§6). GBNF 문법 또는 JSON 스키마로 `command`를
  화이트리스트 4종의 enum으로, `quantity`를 `minimum`/`maximum`이 있는 정수로 박아두면
  **범위 밖 값이나 미등록 명령은 생성 자체가 불가능해집니다.** 걸러지는 것이 아니라 토큰
  샘플링에서 배제됩니다. 사후 거부는 플레이어 발화가 툴 사용을 조종하려 할 때 실효가 없습니다
- **SSE 스트림을 `chat_delta`로 중계**하되 토큰 단위가 아니라 **50ms 또는 토큰 몇 개 단위로 묶어서**
  보내세요. 묶지 않으면 클라이언트의 틱 예산과 큐 상한을 정상 동작 중에 압박합니다
- **`/health`로 모델 로딩 중과 준비 완료를 구분**하고, 준비 전에 온 `chat`은 `server_busy`로
  답하세요(§5). 이것이 그 에러 코드의 실제 발생 지점입니다
- **슬롯이 포화되면 큐에 넣되 큐 깊이에 상한**을 두세요. GPU가 하나이므로 이 상한이 서버측
  과부하 방어의 실제 구현 지점입니다. 초과분은 `rate_limited`를 해당 `chat.id`와 함께 회신합니다
- **KV 캐시 슬롯 저장·복원을 대화 저장소 설계와 함께 보세요.** 재접속 시 캐시째 복원하면 맥락
  재구성 비용이 사라집니다 — `player_id`별 자격 증명 저장(§3)과 같은 수명주기입니다
- **NPC 대사에서 마크다운·툴 호출 흔적·사고 과정을 제거**한 뒤 `text`에 넣으세요(위 절대 규칙 5)

> llama.cpp의 정확한 파라미터·엔드포인트 이름은 버전에 따라 다릅니다. 사용하는 빌드의
> `server` 문서로 확인하세요. 위 항목은 **필요한 기능을 규정할 뿐 API 이름을 확정하지 않습니다.**

**진짜로 재량인 것**: 모델 파일 선택과 샘플링 파라미터, 시스템 프롬프트 설계, 대화 히스토리
보관 정책과 컨텍스트 윈도 관리 방식. 그 외 아키텍처는 위에서 고정입니다.

### 7. 에러 코드 (§5)

코드 10종과 각각의 **연결 유지/종료 + 클라이언트 반응**이 계약으로 정의되어 있습니다.
**목록은 닫혀 있습니다 — 코드를 지어내지 마세요.** 해당 없는 상황은 `internal_error`에 상세를 `message`로 넣으세요.

진행 중인 턴 때문에 발생한 에러라면 **`id`를 함께 실어** 보내세요. 클라이언트가 60초 타임아웃을
기다리지 않고 그 턴을 즉시 실패시킬 수 있습니다.

### 8. 운영

- 인증된 세션 단위 레이트 리밋과 추론 큐 깊이 제한. **클라이언트의 자체 제한은 클라이언트를 보호할 뿐 서버를 보호하지 않습니다**
- `ping`에 `pong` 회신, 침묵한 연결 회수. **클라이언트가 먼저 알아채리라 기대하지 마세요**
  (스펙 §9 참고 — 현재 클라이언트는 keepalive를 아직 구현하지 않았습니다)
- 서버 측 타임아웃은 클라이언트 ping 간격(기본 20초)보다 충분히 위로 잡아 정상 클라이언트가 회수되지 않게 하세요
- **상시 구동**: 개발 중에는 콘솔에서 띄워도 되지만, 배포 시에는 Windows 서비스로 등록하는 편이
  낫습니다(NSSM, `sc.exe create`, 또는 런타임 자체의 서비스 호스팅). 자격 증명 저장소가 **재시작을
  넘어 유지되어야** 하므로(§3), 프로세스를 어떻게 띄우든 데이터 경로가 사용자 프로필 밖의
  안정적인 위치인지 확인하세요
- **경로 처리**: 저장소·인증서·로그 경로에 공백이 들어가는 것이 Windows에서는 기본값입니다
  (`C:\Program Files\...`). 경로를 문자열로 이어 붙이지 말고 런타임의 경로 API를 쓰세요

---

## ⚠️ 현재 UE5 클라이언트 상태 (엔드투엔드 테스트 전 필독)

**UE5 클라이언트는 아직 스펙의 일부만 구현되어 있습니다.** 서버를 완성해도 곧바로 붙지 않습니다.
호환성 문제가 아니라 **아직 만들지 않은 기능**이며, 플러그인 쪽 별도 작업으로 진행 중입니다:

- `identify`에 `protocol_version`도 `session_token`도 없고, 클라이언트가 `player_id`를 스스로 만듭니다
- `chat_delta`와 `action_event` 메시지 타입이 없습니다
- 서버 `ping`에 `pong`은 회신하지만, 스스로 ping을 보내거나 침묵 연결을 끊지 않습니다
- **`chat` 응답 타임아웃도 아직 없습니다.** 서버가 턴을 조용히 버리면 지금은 60초 후 실패가 아니라
  **영원히 "생각 중..."에 머뭅니다.** 스펙(§9)의 60초는 구현 예정 동작입니다
- **종료성 에러 코드를 아직 구분하지 않습니다.** `session_taken_over`를 받아도 무한 재연결하므로,
  서버가 세션 탈취를 켜는 시점에 클라이언트 대응이 없으면 eviction war가 실제로 발생합니다
- TLS 설정 키는 있으나 이를 소비하는 전송 구현이 없습니다 (평문 TCP만)

따라서 **서버 개발 중 검증은 레퍼런스 클라이언트와 스텁으로 하세요.** UE5 클라이언트의 v2 대응은
별도 작업(플러그인 Phase 3~4)으로 진행되며, 그것이 끝나야 실제 게임과의 통합 검증이 가능합니다.

---

## 진행 방식 요청

1. 첨부한 `ue5-socket-protocol.md`를 먼저 읽고 **프레이밍·메시지 타입·필드·에러 코드·상태 전이를
   요약해서 보여줘.** 오해가 없는지 먼저 확인하고 싶음. 특히 다음 네 가지를 어떻게 이해했는지 명시할 것:
   - 신원 발급과 재접속 검증의 차이
   - 세션 탈취 시 기존 연결 처리 순서
   - 턴 직렬화와 `chat_delta` 배칭 규칙
   - `action_result`(접수)와 `action_event`(완료)의 분리
2. 승인 후, **프레이밍 + TLS 종단 + 연결 수명주기**부터 구현.
   **LLM을 붙이기 전에 와이어가 먼저 맞아야 합니다** — 이 단계는 고정 문자열을 되돌려주는
   더미 응답으로 끝까지 검증할 수 있고, 그래야 나중에 문제가 생겼을 때 프로토콜 탓인지
   추론 탓인지 갈라집니다
3. **신원 발급·검증·영구 저장**과 세션 탈취 구현
4. **대화 턴 처리** — 직렬 큐, llama-server SSE → `chat_delta` 배칭 중계, authoritative `chat_response`
5. **액션 왕복** — `action_request` 푸시, `action_result` 접수 처리, `action_event`의 tool observation 되먹임
6. **에러 코드 10종 전부**를 실제로 내보내는 경로 구현
7. 마지막에 **검증 절차를 체크리스트로 정리**해줘:
   - 파이썬 레퍼런스 클라이언트(`scripts/ue5_test_client.py`)를 **스펙에 맞춰 작성/갱신**
     (TLS, `protocol_version: 2`, 자격 증명 보관·재전송, `chat_delta` 누적).
     자격 증명 왕복을 빼면 `identified` 단계에서 거부되어 테스트 도구로 못 씁니다 (§8)
   - 스펙 §8 "Stub servers worth having" 표의 실패 경로들을 **서버 쪽에서 재현**해 클라이언트 방어를
     확인할 수 있는 스텁 모음
   - 두 클라이언트 동시 접속 → 세션 탈취가 **eviction war 없이** 동작하는지
   - 연속 발화 3건 → 순서대로 하나씩 답하고 교차 전송이 없는지
   - Windows 방화벽 인바운드 규칙과, UE5가 다른 PC에 있다면 `Test-NetConnection <서버IP> -Port 8770`로
     도달 가능 여부부터 확인 (`nc`/`telnet`은 바이너리 길이 프리픽스를 만들 수 없어 프로토콜
     검증에는 못 씁니다 — §8)

---

## 참고 환경 정보

- 서버 플랫폼: **Windows**. 포트 `8770/TCP`
- 클라이언트가 접속할 주소는 **클라이언트 설정**이며 프로토콜의 일부가 아닙니다.
  현재 UE5 프로젝트의 `Config/DefaultGame.ini`는 `Host=192.168.0.111`로 되어 있으니,
  서버를 다른 머신에서 돌리면 그 값을 바꾸거나 `-HermesHost=` 실행 인자로 덮으면 됩니다.
  **UE5와 같은 PC에서 개발한다면 `127.0.0.1`이 가장 편합니다** (방화벽·NAT 문제가 사라짐)
- 클라이언트: Windows / UE 5.8 C++ 플러그인 (NPC **1개체**)
- LLM 백엔드: `llama-server` (llama.cpp). HTTP + SSE로 연동. GPU 1대 기준이므로
  **병렬 슬롯 수가 곧 동시 처리 상한**입니다
- 첨부 파일 2종은 맨 위 "시작 전 필수" 참고

### 시작 전 설치·준비물

| 준비물 | 용도 |
|---|---|
| **Git for Windows** | `openssl.exe`(SPKI 핀 산출, §0)와 Git Bash |
| **Python 3** | 레퍼런스 테스트 클라이언트 (`nc`/`telnet`은 바이너리 길이 프리픽스를 못 만들어 대체 불가) |
| **llama.cpp `llama-server`** + 모델 파일(GGUF) | LLM 백엔드. **GBNF/JSON 스키마 제약과 SSE 스트리밍을 지원하는 빌드**여야 합니다 |
| 구현 언어·런타임 | 위 선택 기준 3가지 참고 |

설치는 아니지만 먼저 해둘 것:

- **방화벽 인바운드 규칙** — `New-NetFirewallRule -DisplayName "Hermes 8770" -Direction Inbound -LocalPort 8770 -Protocol TCP -Action Allow`. 접속 실패의 1순위 원인입니다
- **개발용 인증서** — `New-SelfSignedCertificate`. 단 클라이언트는 TLS 실패 시 평문으로 폴백하지 않으므로, 켜는 순간부터 유효해야 합니다
- **llama-server 기동 확인** — 모델 로딩에 시간이 걸리므로 `/health`가 준비 완료를 알리는 시점을 먼저 눈으로 확인해두세요. 이것이 `server_busy`를 언제 내보낼지의 기준이 됩니다

### 재량 / 고정 구분

| 재량 | 고정 |
|---|---|
| 모델 파일, 샘플링 파라미터 | 에이전트는 Hermes, 백엔드는 llama-server |
| 시스템 프롬프트 설계 | 소켓 프로토콜 전체 (첨부 스펙) |
| 대화 히스토리 보관 정책, 컨텍스트 윈도 관리 | 액션 카탈로그 4종, 에러 코드 10종 |
| `chat_id` 발급 방식, 인증서 발급 정책 | 단일 NPC, 턴 직렬화, 서버 발급 신원 |
| 서버 측 레이트 리밋 수치 | 문법 제약 디코딩(수단은 재량, 존재는 필수) |
